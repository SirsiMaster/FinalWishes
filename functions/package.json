{
  "name": "finalwishes-functions",
  "version": "2.0.0",
  "description": "FinalWishes Cloud Functions \u2014 Firestore triggers only (HTTP API lives in Go on Cloud Run)",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^6.1.1",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.2.2",
    "googleapis": "^144.0.0"
  },
  "scripts": {
    "lint": "echo 'No lint configured'",
    "test": "jest --verbose",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "private": true,
  "devDependencies": {
    "jest": "^30.4.2"
  },
  "overrides": {
    "qs": "^6.15.2",
    "uuid": "^11.1.1"
  }
}
