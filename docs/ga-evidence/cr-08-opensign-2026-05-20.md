# CR-08 OpenSign Evidence - 2026-05-20

## Criterion

CR-08: OpenSign templates live for 4 directives.

Bar: OpenSign template registry contains at least four active templates pinned to product IDs in code, covering Living Will, Healthcare POA, Financial POA, HIPAA Authorization.

## Verdict

`NOT MET / OWNER-GATED + SCOPE MISMATCH`

OpenSign template creation requires account-owner and legal-review actions. The current product code also does not yet model the exact four CR-08 directive names end to end: the Illinois advance-directive API currently exposes Health Care POA, Living Will, Mental Health Treatment Preference, and POLST, while CR-08 requires Living Will, Healthcare POA, Financial POA, and HIPAA Authorization. The implementation cannot honestly mark CR-08 MET until those product IDs and template IDs are aligned.

## Capture

- Author: `codex-finalwishes`
- Captured at: `2026-05-20T20:31:21Z`
- Head commit at capture: `a473bd9`
- Governing router plan: `/Users/thekryptodragon/Development/sirsi-pantheon/.agents/idea-router/proposals/20260520-claude-finalwishes-owner-readiness-plan.md`
- Governing review conditions: `/Users/thekryptodragon/Development/sirsi-pantheon/.agents/idea-router/reviews/20260520-codex-finalwishes-ga-workstream-plan-batch-review.md`
- Governing ADR: `docs/ADR-015-OPENSIGN-CONVERGENCE.md`

## Owner Checklist

1. Confirm CR-08 scope remains four v1.0.0 templates, not 12 state variants.
2. Have counsel approve final template language for:
   - Living Will
   - Healthcare Power of Attorney
   - Financial Power of Attorney
   - HIPAA Authorization
3. Upload the four approved templates in OpenSign at `sign.sirsi.ai`.
4. Record each OpenSign template ID, template name, version/hash, signer role name, and approval date.
5. Confirm each template has a signer role compatible with the Go API payload (`Signer`) or route a small backend change to support per-template role names.

## Repo Findings

- Existing personal directive signing flow sends dynamic template IDs of the form `directive-${directive.type}` from `web/src/routes/estates.$estateId.directives.tsx`.
- Existing personal directive types are `ethical_will`, `funeral_preferences`, `final_message`, and `care_instructions`.
- Existing Illinois legal advance-directive API returns `hcpoa`, `living_will`, `mental_health`, and `polst` from `api/internal/probate/directives.go`.
- The CR-08-required `financial_poa` and `hipaa_authorization` product IDs are not currently present in the legal advance-directive API.
- The evidence convention requires the OpenSign wiring module to exist at `web/src/lib/directives.ts` or an equivalent registry path before CR-08 can be marked MET.

## Verification Commands Still Required

After the four live template IDs are available and code wiring is committed:

```bash
rg -n "OPENSIGN_TEMPLATE_IDS|living_will|healthcare_poa|financial_poa|hipaa_authorization" web/src api/internal
git rev-parse --short HEAD
```

End-to-end checks still required:

1. Create or select a test paid estate.
2. Generate each of the four CR-08 directives.
3. Start signing through the OpenSign proxy.
4. Complete each signing ceremony.
5. Verify the signed PDF arrives in the vault and the estate audit trail records the event without raw PII in public logs.

## Required Final Evidence

The MET artifact must include:

1. Four OpenSign template IDs.
2. Template names and version hashes.
3. Code-side wiring commit SHA and exact registry path.
4. Screenshots or sanitized transcripts proving completed signing flows.
5. Confirmation that no raw PII/HIPAA data appears in client logs, public telemetry, or URLs.

Create a new dated `docs/ga-evidence/cr-08-opensign-<YYYY-MM-DD>.md` when legal review, template upload, code wiring, and end-to-end signing verification are complete. Do not mutate this blocked capture into a MET artifact.
