# CR-05 Domain Evidence - 2026-05-20

## Criterion

CR-05: Custom domain DNS + TLS.

Bar: `finalwishes.app` resolves to Firebase Hosting via A/AAAA records; `https://finalwishes.app/` returns HTTP 200 with valid TLS cert chain.

## Verdict

`NOT MET / OWNER-GATED`

DNS registrar and Firebase Hosting custom-domain actions require the account owner. Code-side readiness is partially complete: production CORS already allows `https://finalwishes.app`, and this pass added `APP_BASE_URL` so generated Stripe checkout and portal return URLs can move to `https://finalwishes.app` after DNS + TLS are live without editing code again.

## Capture

- Author: `codex-finalwishes`
- Captured at: `2026-05-20T20:31:21Z`
- Head commit at capture: `a473bd9`
- Governing router plan: `/Users/thekryptodragon/Development/sirsi-pantheon/.agents/idea-router/proposals/20260520-claude-finalwishes-owner-readiness-plan.md`
- Governing review conditions: `/Users/thekryptodragon/Development/sirsi-pantheon/.agents/idea-router/reviews/20260520-codex-finalwishes-ga-workstream-plan-batch-review.md`

## Owner Checklist

1. Open Firebase Console -> Hosting -> Add custom domain.
2. Enter `finalwishes.app`.
3. Add the Firebase-provided TXT verification record at the registrar.
4. Replace apex A records with the exact A records Firebase shows for `finalwishes.app`.
5. Add `www` CNAME to `finalwishes-prod.web.app` if Firebase requests the `www` host.
6. Wait for Firebase certificate provisioning to show active.
7. After the checks below pass, set Cloud Run `APP_BASE_URL=https://finalwishes.app`.

Do not change Stripe webhook URLs in code. If Stripe has a webhook endpoint configured in the dashboard, update it there only after the public domain is live.

## Verification Commands Still Required

```bash
dig finalwishes.app A
dig finalwishes.app AAAA
curl -sSI https://finalwishes.app/
openssl s_client -connect finalwishes.app:443 -servername finalwishes.app </dev/null | openssl x509 -noout -dates -subject -issuer
```

Expected final state:

- `dig` returns Firebase Hosting records shown by Firebase Console.
- `curl` returns HTTP 200 or the deployed SPA shell with no TLS error.
- `openssl` returns a valid, unexpired certificate for `finalwishes.app`.

## Code-Side Readiness

- `api/cmd/api/main.go` already includes `https://finalwishes.app` in production CORS origins.
- `api/internal/payments/handlers.go` now supports `APP_BASE_URL` for generated Stripe checkout success/cancel URLs and Billing Portal return URLs.
- `.env.example` and `docs/DEPLOYMENT_GUIDE.md` document the new `APP_BASE_URL` setting.

## Next Evidence Artifact

Create a new dated `docs/ga-evidence/cr-05-domain-<YYYY-MM-DD>.md` when DNS and TLS are live. Do not mutate this blocked capture into a MET artifact.
