// FinalWishes Mobile — Dashboard Tab
import { View, Text } from 'react-native';

export default function DashboardTab() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Dashboard — Coming Soon</Text>
    </View>
  );
}
