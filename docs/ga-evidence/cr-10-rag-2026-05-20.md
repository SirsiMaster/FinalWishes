# CR-10 RAG Evidence — 2026-05-20

Criterion: Full RAG with state-specific legal corpus.

Author: codex-finalwishes  
Capture timestamp: 2026-05-20T21:45:00Z  
Verdict: NOT MET / ARCHITECTURE FOUNDATION READY

## Evidence Captured

- ADR created: `docs/ADR-044-LEGAL-RAG-CORPUS.md`.
- Schema created: `api/internal/guidance/schema.sql`.
- Runtime foundation created: `api/internal/guidance/rag.go`.
- Shepherd chat now returns `citations` when legal RAG is configured and abstains when no approved corpus chunk supports a legal answer.
- Manifest seeded: `docs/legal-corpus/manifest.md`.
- Probe-set skeleton seeded: `docs/legal-corpus/probe-set.json`.

## Verification Commands

Pending final command transcript after tests complete in this session:

```bash
cd api && go test ./internal/guidance ./cmd/rag-eval
cd api && go run ./cmd/rag-eval -probe-set ../docs/legal-corpus/probe-set.json
```

## Current Guardrails

- Corpus storage must use Cloud SQL/AlloyDB PostgreSQL with pgvector, not the ADR-037 PII vault database.
- Default embedding model is `gemini-embedding-001` at 3072 dimensions based on Google Cloud documentation checked on 2026-05-20.
- GA corpus sources must be official primary sources only.
- A Shepherd legal answer without supporting chunks must abstain.

## Remaining Before MET

- Ingest authoritative IL/US/MD/MN source text with source URLs, retrieval timestamps, checksums, chunk counts, and verified dates.
- Enable `pgvector` on the target corpus database.
- Generate embeddings and populate `legal_corpus_chunks`.
- Run a 50-question held-out probe set and record hallucination rate at or below 2%.
